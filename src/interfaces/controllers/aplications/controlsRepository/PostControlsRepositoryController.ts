import {Controller} from "../../../gateway/controller/controller";


export class PostControlsRepositoryController implements Controller {
    handle(req: any): Promise<any> {
        return Promise.resolve(undefined);
    }

}
